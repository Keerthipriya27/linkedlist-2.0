class Solution:
    def removeAnagrams(self, words: list[str]) -> list[str]:
        res = []
        for w in words:
            if res and sorted(res[-1]) == sorted(w):
                continue
            res.append(w)
        return res
